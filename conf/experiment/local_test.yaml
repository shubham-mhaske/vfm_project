# @package _global_

defaults:
  - path_sam2_focal_stain

experiment:
  name: local_test

# Override settings for a quick local dry run
scratch:
  num_epochs: 1
  train_batch_size: 1
  num_train_workers: 0 # Use 0 for local debugging to avoid multiprocessing issues

trainer:
  accelerator: cpu
  max_epochs: 1
  distributed:
    backend: gloo
  optim:
    amp:
      enabled: false
    